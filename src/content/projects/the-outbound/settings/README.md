# Settings

Places in *The Outbound*. Each named setting gets its own subdirectory with a `scrapbook/` for premise notes, atmosphere, and reference material.

Current sub-nodes:

- `libertardistan/` — the breakaway "free state" territories
- (more to come: the launch sites, the ships themselves, life on the receiving worlds — to whatever extent there is one)
